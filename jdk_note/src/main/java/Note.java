/**
 * @date 2022/1/18 8:40
 */
public class Note {
    /**
     * jdk1.8新增的功能
     *      接口的新特性，新增静态成员函数和默认函数，
     *
     *      **新增lambda表达式，并且内置了四个函数式接口
     *      ————》lambda的简化形式
     *      ————》lambda再次简化，用方法引用来替换
     *
     *      **string流操作，用来对集合的筛选等
     *      ————》其中的方法，终结方法，非终结方法以及并行stream流
     *      ————》fork join框架
     *
     *      Optional用来替换null值
     *
     *      新的时间日期类LocalDate等
     *
     *      其中，lambda和stream流最为重要
     *
     */
}
