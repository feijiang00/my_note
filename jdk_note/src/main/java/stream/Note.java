package stream;

/**
 * @date 2022/1/18 9:19
 */
public class Note {
    /**
     * stream流，真的很方便。
     * 获取流两种方式
     *      1.根据集合
     *      2.根据流中的of方法
     *      Stream<String> stream6 = Stream.of("aa", "bb", "cc");
     *
     * stream流中的方法：
     *      1.终结方法，foreach，collect，count
     *      2.非终结方法 filter，skip等
     *
     * stream流，收集，collect
     *      聚合，分组，等，
     *      ————————————到这里没怎么看了，这章
     *
     * 并行stream流
     *      直接获取并行流: parallelStream()
     *      将串行流转成并行流: parallel()
     *
     *  Fork_join框架，并行流实现的原理
     */
}
