package java刘意.IO;

/**
 * @date 2021/12/3 10:41
 */
public class sumalary {
    /**
     * 最先出来的是文件流，因为不论是字节流还是字符流都需要文件流
     * 因此：
     * File流出现了，获取一个文件夹对象，或者一个文件对象。其中的方法有很多，例如获取改文件对象的字符串，判断改文件对象是否存在，
     * 并且获取该文件夹下面的所有文件和文件夹对象等等方法
     *
     * 有了文件对象，就可以进行输出输出了，于是出现了字节流，但是字节流读出写入太慢了， 并且会操作底层，所以出现了字节缓冲流
     *
     * 后面因为字节流对于文本操作有需要加上编码很麻烦，出现了字符流，字符流=字节流+编码
     */
}
